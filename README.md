# 📝 Exam Management System (C++)

A console-based exam management system implemented in C++. The project handles examination workflows such as presenting descriptive and multiple-choice questions, collecting answers, and calculating results, with clear logic and file handling.

##  The code handels:
- Interactive command-line exam interface
- Multiple-choice questions
- Automatic score calculation
- Persistent storage using text and CSV files

## 📂 Project Structure
- `sourceCode.cpp` — main application logic
- `data.txt` — input and configuration data
- `grades_db.csv` — stored exam results
- `reports/` — generated exam reports
- `sheets/` — submited exam sheets
- `desc_answ/` — saved descriptive answers (if applicable)


